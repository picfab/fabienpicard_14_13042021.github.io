/**
 * @function
 * @category Redux
 * @param  {object} props
 * @prop  {string} props.employees The state with the list of employees
 * @return {array} The state with the list of employees
 */
// eslint-disable-next-line import/prefer-default-export
export const employeesSelector = ({ employees }) => employees
